# Futures Screener (Binance USDT-M) 📊🚀

## Описание

Быстрый визуальный скринер плотностей ордерных книг на Binance Futures (USDT-M). Показывает крупные лимитные уровни рядом с текущей ценой (markPrice), сортирует и подсвечивает «MM» — уровни с высокой силой ликвидности.

### Архитектура
- **server/** — Node.js + Fastify API, ходит в Binance Futures REST
- **app/** — статический HTML/JS/CSS (без сборщика)
- **Порт:** 3200 (`process.env.PORT || 3200`)

## Быстрый старт

```bash
cd server
node index.js
# Открыть http://127.0.0.1:3200 в браузере
```

### Требования
- Node.js 18+
- npm 9+

## API Endpoints

| Метод | Путь | Описание |
|-------|------|----------|
| GET | `/health` | healthcheck |
| GET | `/symbols` | список всех USDT-PERP символов |
| GET | `/depth/:symbol` | стакан по символу |
| GET | `/densities/simple` | **главный** — плотности с MM-флагом |

### `/densities/simple` — параметры

| Параметр | Default | Описание |
|----------|---------|----------|
| `symbols` | (все) | список через `,` (например `BTCUSDT,ETHUSDT`) |
| `minNotional` | `50000` | минимальный notional (price * qty) |
| `depthLimit` | `100` | сколько уровней стакана брать |
| `windowPct` | `1.0` | окно вокруг цены (±%) для поиска плотностей |
| `concurrency` | `6` | параллельность запросов к Binance |
| `limitSymbols` | `0` | ограничить количество символов (для теста) |
| `mmMode` | `false` | показать только MM-уровни |
| `mmMultiplier` | `4` | множитель для флага `isMM` |

## Текущий статус

🟢 **Phase 0: MVP — готов**
- ✅ API `/densities/simple` работает
- ✅ Сортировка по `notional` (без scorинга)
- ✅ MM-флаг `isMM` (без scorинга)
- ✅ UI: таблица с автообновлением (каждые 10 сек)
- ⏳ Скоринг (`score`) — планируется в ближайшем PR
- ⏳ Cache (in-memory) — планируется

## Планы (Phase 1+)

- Добавить пресеты: `scalp tight (0.3–0.6%)`, `swing (1–2%)`
- Разделить вкладки: `Top Densities`, `By Symbol`, `Watchlist`
- Алерты: уведомления при приближении цены к уровню
- Скоринг: `score = log10(1 + notional) * exp(-distancePct / decayPct) * boost`

## Ссылки

- [VISION.md](docs/VISION.md) — долгосрочные цели
- [ROADMAP.md](docs/ROADMAP.md) — этапы разработки
- [UI-SPEC.md](docs/UI-SPEC.md) — спецификация UI
