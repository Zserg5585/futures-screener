# ROADMAP — Futures Screener

---

## Phase 0 · Foundation

**Цели:**
- Базовая серверная инфраструктура с мок-данными.
- Подготовка структуры проекта и документации.

**Шаги:**

| #   | Задача                                                        | Артефакты                                                        |
|-----|---------------------------------------------------------------|------------------------------------------------------------------|
| 0.1 | Создать endpoint `GET /api/screener` с мок-данными            | `server/index.js` — роут; `server/modules/mock.js` — генератор  |
| 0.2 | Минимальный фронтенд-скелет в `app/` (placeholder-таблица)   | `app/index.html`, `app/app.js`, `app/styles.css`                |
| 0.3 | Документация: README с инструкцией запуска                    | `README.md` обновлён                                             |

---

## Phase 1 · MVP Screener

**Цели:**
- Рабочий скринер: таблица с данными, сортировка, базовые фильтры.
- Горизонтальный скролл таблицы (Desktop Wide + Mobile Variant A).

**Шаги:**

| #   | Задача                                                        | Артефакты                                                        |
|-----|---------------------------------------------------------------|------------------------------------------------------------------|
| 1.1 | UI-таблица в `app/` с горизонтальным скроллом                 | `app/app.js` — компонент таблицы                                 |
| 1.2 | Сортировка по колонкам (клик на заголовок)                    | `app/app.js` — логика сортировки                                 |
| 1.3 | Базовые фильтры (по имени, рынку, мин/макс цена)            | `app/app.js` — UI фильтров; стили в `app/styles.css`            |
| 1.4 | Подключение таблицы к `GET /api/screener`                    | `app/app.js` — fetch; `server/index.js` — CORS                  |
| 1.5 | Мобильный layout (Mobile Variant A): адаптивная таблица      | `app/styles.css` — media-queries                                 |

---

## Phase 2 · Hardening

**Цели:**
- Реальный источник данных вместо моков.
- Обработка ошибок, логирование, стабильность.

**Шаги:**

| #   | Задача                                                        | Артефакты                                                        |
|-----|---------------------------------------------------------------|------------------------------------------------------------------|
| 2.1 | Подключение реального источника данных (API биржи)           | `server/modules/binance/` или аналог — коннектор                 |
| 2.2 | Retry / backoff при сбоях источника                          | `server/modules/` — утилита retry                                |
| 2.3 | Серверное логирование (pino уже в зависимостях)              | `server/index.js` — настройка логгера                            |
| 2.4 | Обработка ошибок: UI показывает статус загрузки / ошибку     | `app/app.js` — error-state; `app/styles.css`                    |
| 2.5 | End-to-end тесты и финальная документация                    | `tests/`, обновлённый `README.md`                                |

---

## Стратегия разработки (привязка)

1. **Step 1** — server endpoint `/api/screener` с мок-данными → Phase 0, шаг 0.1
2. **Step 2** — UI таблица в `app/` (горизонтальный скролл, сортировка, базовые фильтры) → Phase 1, шаги 1.1–1.5
3. **Step 3** — подключение реального источника данных → Phase 2, шаг 2.1

## Phase 1 (MVP Screener) - Densities (specific)
- Goal: MVP implementation of Densities tab integrated with backend mock data.
- Alignment: complements existing backend density module; frontend to be implemented in upcoming PR.
